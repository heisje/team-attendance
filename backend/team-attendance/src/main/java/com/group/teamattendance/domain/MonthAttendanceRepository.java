package com.group.teamattendance.domain;

import org.springframework.data.jpa.repository.JpaRepository;

public interface MonthAttendanceRepository extends JpaRepository<MonthAttendance, Long> {
    
}
